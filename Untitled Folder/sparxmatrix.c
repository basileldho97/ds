#include <stdio.h>
int main()
{
	int p1[10][10],p2[10][10],p[10][10],n,n1,n2,i,j,sn;
        
	printf("Enter the degree of first polynominal");  //first polynominal
        for(i=0;i<n1;i++)
          for(j=0:j<2:j++)
            {
           if(j==0)        
             {
                 printf("Enter the degree of first polynominal");
            scanf("%d",p1[i][j]);

        printf("Enter the coeff. first polynominal");
        for(i=n1;i>=0;i--)
          scanf("%d",&p1[i]);




        printf("Enter the degree of second polynominal");   //second polynominal
        scanf("%d",&n2);

        printf("Enter the coeff. second polynominal");
        for(i=n2;i>=0;i--)
          scanf("%d",&p2[i]);
